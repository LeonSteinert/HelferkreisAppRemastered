import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Helferkreis-App-Remastered',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
